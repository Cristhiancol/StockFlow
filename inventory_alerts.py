from fastapi import FastAPI, BackgroundTasks\nfrom typing import List\n\napp = FastAPI()\n\n# Define alert types\nALERT_TYPES = {\n    'stock_at_zero': 'Stock is at zero.',\n    'low_inventory': 'Inventory is low.',\n    'expired_msds': 'MSDS sheets have expired.',\n    'auto_purchase_orders': 'Auto purchase orders triggered.'\n}\n\n# Simulated inventory data\ninventory = {\n    'item1': {'quantity': 5, 'msds_expiry': '2022-12-01'},\n    'item2': {'quantity': 0, 'msds_expiry': '2023-05-01'},\n    'item3': {'quantity': 10, 'msds_expiry': '2025-11-01'}\n}\n\n# Function to check inventory and send alerts\ndef check_inventory():\n    alerts = []\n    for item, details in inventory.items():\n        if details['quantity'] == 0:\n            alerts.append(ALERT_TYPES['stock_at_zero'])\n        elif details['quantity'] < 10:\n            alerts.append(ALERT_TYPES['low_inventory'])\n        if details['msds_expiry'] < '2026-04-04':  # Check against current date\n            alerts.append(ALERT_TYPES['expired_msds'])\n    return alerts\n\n@app.get('/check_inventory')\ndef check_inventory_endpoint(background_tasks: BackgroundTasks):\n    alerts = check_inventory()\n    if alerts:\n        background_tasks.add_task(send_alerts, alerts)\n    return { 'messages': alerts }\n\ndef send_alerts(alerts: List[str]):\n    for alert in alerts:\n        print(alert)  # Replace with actual alerting mechanism\n\n# You can later add auto purchase order logic separately, triggered by the alerts if needed.